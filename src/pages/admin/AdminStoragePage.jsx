import StorageManager from "../../components/admin/StorageManager";

/**
 * Página de administración de imágenes (Firebase Storage)
 */
export default function AdminStoragePage() {
  return <StorageManager />;
}
